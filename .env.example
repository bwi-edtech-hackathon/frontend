# === API Configuration ===
# Absolute URL to the backend. If left blank, it defaults to the current host.
VITE_API_BASE_URL=https://uneccentrically-flagrant-nelson.ngrok-free.dev

# === Mock Mode ===
# Set to 'true' to use local mock fixtures (useful for UI development without a backend).
VITE_USE_MOCK_DATA=false
